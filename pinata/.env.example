# Preferred: scoped Pinata JWT (Pinata dashboard -> API Keys)
PINATA_JWT=

# Legacy alternative (use instead of JWT):
# PINATA_API_KEY=
# PINATA_SECRET_API_KEY=

# Optional: dedicated gateway host. Default: gateway.pinata.cloud
# PINATA_GATEWAY=your-gateway.mypinata.cloud
